[package]
name = "ssl-certs"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "Apache-2.0 OR ISC OR MIT"
readme = "README.md"

[dependencies]
pki-types = { package = "rustls-pki-types", version = "1.10", features = ["std"] }
